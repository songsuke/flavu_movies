<% @preference.each do |n1| %>
<%= form_for n1 do |f| %>
  <% if n1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(n1.errors.count, "error") %> prohibited this n1 from being saved:</h2>

      <ul>
      <% n1.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <input id="genre_preference_genre_id" name="genre_preference[genre_id]" type="number" value="">
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<%end%>
<%end%>