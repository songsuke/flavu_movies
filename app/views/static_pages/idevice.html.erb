	<script> 
		var userAgent = window.navigator.userAgent;
		if (userAgent.match(/iPhone/i) || userAgent.match(/iPad/i)) {	
			var baseUrl = "flavumovies://?reset_password_token=";
			var token = "<%= @reset_password_token %>";
			window.location = baseUrl + token;
		}
	</script>